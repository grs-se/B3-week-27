import { ModifyDomDirective } from './modify-dom.directive';

describe('ModifyDomDirective', () => {
  it('should create an instance', () => {
    const directive = new ModifyDomDirective();
    expect(directive).toBeTruthy();
  });
});
